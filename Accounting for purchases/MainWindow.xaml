<Window x:Class="Accounting_for_purchases.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Accounting_for_purchases"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="1200">
    <Window.DataContext>
        <local:MainViewModel></local:MainViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" >
            <Button Command="{Binding OpenOrder}" Content="Заказы" Height="35" Width="125" Margin="10,0,10,0"></Button>
            <Button Command="{Binding OpenDirectory}" Content="Справочник" Height="35" Width="125" Margin="10,0,10,0"></Button>
            <Button Content="Отчет" Command="{Binding OpenReport}" Height="35" Width="125" Margin="10,0,10,0"></Button>
        </StackPanel>
        <ContentControl Grid.Row="1" Content="{Binding MainControl}" ></ContentControl>
    </Grid>
</Window>
